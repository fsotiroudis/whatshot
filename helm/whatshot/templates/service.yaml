apiVersion: v1
kind: Service
metadata:
  name: {{ include "whatshot.fullname" . }}
  labels:
    {{- include "whatshot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "whatshot.selectorLabels" . | nindent 4 }} 